language: node_js
env:
  global:
    - NODE_ENV=TEST
node_js:
  - "lts/*"
services:
  - docker
cache:
  - "node_modules"
before_install:
  - touch .env.dev
  - sh script/ci.sh
install:
  - npm i -g npx
  - npm i
script:
  - npm test